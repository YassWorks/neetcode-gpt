[project]
name = "gpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=1.24.0",
    "torch>=2.0.0",
    "torchtyping>=0.1.4",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.16.1",
]
